#Microsoftaca

## How to use
First install dependencies in a venv
```bazaar
pip install .
```

Then Use:
```bazaar
microsoftaca --help
```

## Alternatives
install via `requirements.txt` then use:
```bazaar
python3 cli.py --help
```